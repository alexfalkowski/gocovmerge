// Command gocovmerge merges one or more Go coverage profiles into a single
// standard profile.
//
// It accepts explicit profile paths or recursively discovers them with `-d`,
// then writes the merged profile to stdout or to a file selected with `-o`.
package main
